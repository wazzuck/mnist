# For direct local connection to Railway DB
DATABASE_URL=postgresql://user:password@host:port/db?sslmode=require

# OR for local Docker development
DB_HOST=localhost
DB_NAME=mnist
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432
